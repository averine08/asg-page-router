/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // devIndicators: false,
    images: {
      domains: ["fakestoreapi.com"],
    },
};

export default nextConfig;
